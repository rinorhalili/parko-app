# Contributing

Use focused changes and keep frontend, backend, and mobile checks green.

Common commands:

- `npm run check`
- `npm test`
- `npm run backend:build`
- `npm run backend:test`
- `npm run typecheck --prefix mobile`

Add tests near the behavior being changed and avoid unrelated formatting churn.
